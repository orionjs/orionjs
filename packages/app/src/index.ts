import Model from './Model'
import modelToSchema from './Model/modelToSchema'
import {crudResolvers, paginatedResolver, tokenPaginatedResolver} from './resolvers'

export {modelToSchema, Model, crudResolvers, tokenPaginatedResolver, paginatedResolver}
