{
  "license": "MIT",
  "name": "orionjs",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "bun run scripts/test.ts",
    "build": "bun run scripts/build.ts",
    "first-install": "bun install --ignore-scripts",
    "prepare": "husky"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@changesets/cli": "^2.28.1",
    "@types/node": "^22.13.10",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "oxc-transform": "^0.56.5",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "rambdax": "^11.3.1"
  },
  "lint-staged": {
    "*.{js,ts,tsx,json}": [
      "biome check --write --no-errors-on-unmatched"
    ]
  }
}
